# VERSIONS — Pinned package versions for sandbox-env
# This is a human-readable reference. NOT sourced by scripts.
# Update versions here as a reference, then update the corresponding files.
#
# Format: package=version        # file where it's pinned

# Base image
node=24.20-slim                   # base/Dockerfile (FROM)

# Base tools (Dockerfile)
claude-code=2.1.236               # base/Dockerfile — npm `stable` dist-tag as of 2026-09-06
git-delta=0.19.2                  # base/Dockerfile
gosu=1.19                         # base/Dockerfile
gh=apt-managed                    # base/Dockerfile — version managed by GitHub's apt repo

# Go (features/go.sh)
go=1.27.1                         # features/go.sh
golangci-lint=2.13.2              # features/go.sh

# GitLab CLI (features/glab.sh)
glab=1.116.0                      # features/glab.sh

# Rust (features/rust.sh)
rust=stable                       # features/rust.sh — rustup stable channel
cargo-watch=8                     # features/rust.sh (major version pin)
cargo-edit=0.13                   # features/rust.sh (major.minor pin)

# Ollama (features/ollama.sh + cli/sandbox)
ollama=0.33.3                     # features/ollama.sh (via install script)
ollama-image=0.33.3               # cli/sandbox cmd_models (Docker image)

# AWS CLI (features/aws.sh)
aws-cli=2.36.40                   # features/aws.sh

# Google Cloud SDK (features/gcloud.sh)
gcloud=apt-managed                # features/gcloud.sh — version managed by Google's apt repo

# npm global packages (features/node-extra.sh)
pnpm=10                           # features/node-extra.sh (major version pin)
yarn=1.22                         # features/node-extra.sh (major.minor pin)
tsx=4                              # features/node-extra.sh (major version pin)
npm-check-updates=17              # features/node-extra.sh (major version pin)

# pip packages (features/llm.sh)
llm=0.34                          # features/llm.sh
llm-anthropic=0.28                # features/llm.sh (formerly llm-claude-3)
llm-ollama=0.17.1                 # features/llm.sh
