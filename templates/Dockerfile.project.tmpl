# Auto-generated by sandbox CLI — do not edit manually
FROM sandbox-base:latest

USER root

# Ensure directories exist for feature scripts
RUN mkdir -p /etc/sandbox/firewall.d /etc/sandbox/services

# Feature scripts
%%FEATURES%%

# Additional apt packages
%%PACKAGES%%

# Custom setup script
%%SETUP%%

# Entrypoint runs as root, then drops to node via gosu
WORKDIR /workspace
